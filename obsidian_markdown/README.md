# Introduction
Code to edit markdown files, mainly ment for Obsidian notes.

The runner can be used to get a file list and run over files to apply the transformations.
# Runner
Gets and runs over a list of markdown files. Applying the transformations.
# Move metadata to properties
Can move info from the metadata section into the properties section of the file. Currently puts everything in a list proprty.

## Possible improvements
- Add different proprty types.
- Create a object Oriented Setup. Using the different properties as entities etc.

